<%= form_for @dict, :url => { :controller => 'pages', :action => 'dictionary_upload' }, :method => "post", :multipart => true do |f| %>
  <%= f.file_field :file %>
  <%= f.submit t("upload") %>
<% end %>

<%= @dict.valid? %>

<% @dict.errors.full_messages.each do |e| %>
  <%= e %>
<% end %>